# /etc/nginx/conf.d/floor-logger-ratelimit.conf
# 30 API requests/min/IP (§6); zone shared across the tracker site.
limit_req_zone $binary_remote_addr zone=fl_api:10m rate=30r/m;
limit_req_status 429;
